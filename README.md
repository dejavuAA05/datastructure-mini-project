
  # Student Marks Search System UI

  This is a code bundle for Student Marks Search System UI. The original project is available at https://www.figma.com/design/pyJvunFsXotbt6Ka5pLY58/Student-Marks-Search-System-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  